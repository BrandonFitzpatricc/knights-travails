# knights-travails

An implementation of an algorithm designed to find the shortest path that a knight piece can take between any two positions on a chess board. This algorithm uses a modified version of breadth-first search, where every 1-move path is checked, then every 2-move path, and so on, until the end position is reached for the first time.